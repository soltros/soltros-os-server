[general]
# MacBook Pro 12,1 optimized thermal settings
# Temperature thresholds (Celsius) - more aggressive for humidity resistance
low_temp = 58        # Start ramping fans sooner than default 63
high_temp = 62       # More aggressive ramp-up than default 66  
max_temp = 78        # Full speed earlier than default 86 (better for humidity)
polling_interval = 1 # Check every second for responsive control

# Fan speeds auto-detected from applesmc:
# min_fan1_speed = 1299 RPM (detected from hardware)
# max_fan1_speed = 6199 RPM (detected from hardware)
